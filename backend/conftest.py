"""Pytest configuration for the backend test suite.

Importing this file causes pytest to add this directory (``backend/``) to
``sys.path``, so tests can ``import app`` whether pytest is invoked from the
repository root (``py -m pytest backend/tests -q``) or from ``backend/``.
"""
